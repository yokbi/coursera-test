namespace HabitTracker.Domain.Enums;

public enum GroupMemberStatus
{
    /// <summary>Invited but has not consented yet; sees nothing about other members.</summary>
    Invited = 0,
    Joined = 1
}
